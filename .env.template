# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5444/church"
TEST_DATABASE_URL="postgresql://postgres:postgres@localhost:5444/church_test"
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres"
POSTGRES_DB="church"

# Better Auth
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"

# CORS
CORS_ORIGIN="http://localhost:3000"

# Web
VITE_SERVER_URL="http://localhost:3000"

# Environment
NODE_ENV="development"

# Debug-only HTTP routes (e.g. redemption's verification-code readback for
# E2E). Never exposed in production regardless of this flag; leave unset or
# "false" in any non-production environment that shouldn't carry them (a
# future public staging, for instance).
ENABLE_DEBUG_ENDPOINTS="true"
