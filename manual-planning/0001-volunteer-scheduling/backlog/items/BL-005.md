# BL-005 — Configurable cooldown for repeated availability reminders

> ## 🅿️ Parked 2026-07-26 — not queued, no rank
>
> Parked by [Is the volunteer-facing cluster a live track?](https://github.com/tiagoluizpoli/church/issues/13). The noisy reminder model was a **deliberate MVP choice**, not an oversight, and no complaint has been recorded against it. `db-availability-check-manager.ts:217-236` sends unconditionally; zero `cooldown` references exist repo-wide. Building anti-spam before the spam is observed inverts the ordering criterion — it is neither pain now nor rework cost, because the notification payload is already keyed on `planningCycleId` + `ministryId`, so the natural `(volunteer, cycle)` cooldown key will still be sitting there whenever it is needed. **Waiting costs nothing.**
>
> **Promotion trigger** — a volunteer complains about reminder frequency, **or** telemetry/a leader report shows repeated resends to the same `(volunteer, cycle)` pair within a short window.

**Status**: Parked
**Feature area**: Volunteer Dashboard (Spec F2 / `manual-planning/0001-volunteer-scheduling/specifications/F2-volunteer-dashboard.md`)

## Summary

Repeated availability reminders should eventually respect a configurable cooldown window, so leaders cannot spam the same volunteer too aggressively for the same Event/Planning Cycle while still preserving the intentionally noisy reminder model.

## Full Context

During the grilling session for Spec F2, the following was decided for MVP:
- Availability reminders are intentionally allowed to be noisier than most other dashboard signals.
- The same reminder need may appear in multiple prominent places in the volunteer experience.
- For MVP, the team is willing to rely on leader discretion rather than building a full anti-spam system immediately.

However, a further refinement was explicitly requested:
- The system should later support a configurable minimum gap between repeated reminders for the same volunteer and scheduling scope.
- The initial implementation can be driven by an environment variable or similar deployment-level configuration.
- In the future, this should move to a better long-term configuration surface if needed.

### What this feature should mean
- If a leader sends an availability reminder to a volunteer, the system records the send time.
- Additional reminders for the same volunteer and cycle are blocked or deferred until the cooldown window has passed.
- The cooldown duration is configurable, rather than hard-coded.
- The cooldown does NOT prevent reminders for different cycles/events or different volunteers.

### Why deferred
- MVP explicitly prioritizes shipping the reminder flow, even if it is somewhat noisy.
- Cooldown rules introduce extra product and infrastructure decisions: where the config lives, how leaders are informed, and whether blocked sends are hidden or explained.
- This is a safety refinement, not a prerequisite for the core volunteer scheduling workflow.

## 017 Reshape Reconciliation Context

- **Still valid but re-scoped**.
- Reminders are now per `PlanningCycle` instead of individual events.
- "Resend" is a first-class leader action.
- The cooldown key becomes `(volunteer, cycle)`, gating the resend.

## Prerequisites for Implementation

1. Reminder sends must be persisted with enough metadata to identify volunteer + PlanningCycle + send timestamp.
2. The reminder-send path must consult a configurable cooldown value before delivering another reminder.
3. Leader-facing feedback must exist when a reminder is blocked or suppressed by cooldown.

## Suggested Approach

- Start with a simple environment-variable-driven cooldown duration, since that was explicitly accepted as a near-term configuration source.
- Scope the cooldown to `(volunteerId, cycleId)` so it does not unintentionally suppress unrelated reminders.
- Surface a concise leader-facing message such as `A reminder was already sent recently for this Planning Cycle`.
- If the reminder system becomes more sophisticated later, migrate the cooldown from environment configuration to a proper application/admin configuration model.
