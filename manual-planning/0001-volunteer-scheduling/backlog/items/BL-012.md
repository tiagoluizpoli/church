# BL-012 — OpenAPI summary/description metadata across controller routes

> ## 📌 Rank 7 · Wave 2 (tier 2) · Issue [#24](https://github.com/tiagoluizpoli/church/issues/24) · **Blocked by [BL-011](./BL-011.md)**
>
> **Why it sits here** — pure documentation debt: no user is blocked. It is tier 2 rather than parked because the docs are a live working surface and the gap widens with every route added.
>
> **Blocked by [BL-011](./BL-011.md)** on a real edge, for one reason: the 67 route descriptions should be written **once**, against the post-split route layout and final tag names, rather than written and then re-homed. It is technically independent — a `description` travels with its route wherever the route moves — but sequencing it first wastes the sweep.
>
> *Scope corrected 2026-07-26 — the gap is larger than this item states.*

**Status**: Ranked — wave 2, rank 7
**Feature area**: API Documentation

## Summary

Fastify route schemas already define `operationId` broadly, but most routes still lack explicit `summary` and `description`. Add those metadata fields consistently so Swagger/Scalar show human-readable operation names and explanations instead of only technical identifiers.

## Full Context

Current state:
- `operationId` is present across the backend controllers.
- `summary` and `description` are largely absent.
- A concrete example now exists in `apps/server/src/api/controllers/church-admin-controller.ts` on `getMinistryServingProfile`, where:
  - `summary` gives the operation a readable label
  - `description` explains what the endpoint returns in Swagger/Scalar

### Validated scope — **7 controllers, 67 routes** (measured 2026-07-25)

- `apps/server/src/api/controllers/church-admin-controller.ts`
- `apps/server/src/api/controllers/admin-leader-controller.ts`
- `apps/server/src/api/controllers/leader-controller.ts`
- `apps/server/src/api/controllers/leader-rostering-controller.ts` *(added by spec 023)*
- `apps/server/src/api/controllers/volunteer-controller.ts`
- `apps/server/src/api/controllers/volunteer-schedule-controller.ts` *(added by spec 023)*
- `apps/server/src/api/controllers/feature-flag-controller.ts`

**67 `operationId`s. Exactly one `summary:` and three `description:` in the entire API surface** — all four in `church-admin-controller.ts`, the very example this item cites. The original scope named 5 controllers; there are now 7. This is a genuinely large sweep, not a quick pass.

> ⚠️ **Sequencing note.** [BL-011](./BL-011.md) splits `admin-leader-controller.ts` into domain controllers and retags all four `['admin']` controllers. Write these descriptions **after** that lands, against the final route homes and tag names.

### Why deferred
- This is documentation polish, not a functional blocker.
- It is best done as a deliberate sweep so naming stays consistent across the full API surface.
- Doing it piecemeal risks uneven wording and mixed conventions.

## Suggested Approach

- Run this as a single documentation pass across all route schemas.
- Keep `operationId` as the machine-stable identifier.
- Add:
  - `summary`: short human-readable operation title
  - `description`: one clear sentence about behavior, scope, or returned data
- Prefer business-language wording over internal method names where helpful.

## Success Criteria

1. Every documented route schema has `operationId`, `summary`, and `description`.
2. Swagger and Scalar display readable operation labels and explanations throughout the API.
3. Naming is consistent across admin, leader, volunteer, and feature-flag surfaces.
