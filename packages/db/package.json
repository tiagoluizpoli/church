{
  "name": "@church/db",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./schema": {
      "types": "./src/schema/index.ts",
      "default": "./src/schema/index.ts"
    },
    "./*": {
      "types": "./src/*.ts",
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "db:migrate": "drizzle-kit migrate",
    "db:reset": "bun --env-file=../../.env src/scripts/reset-db.ts",
    "db:start": "docker compose up -d",
    "db:watch": "docker compose up",
    "db:stop": "docker compose stop",
    "db:down": "docker compose down",
    "db:seed": "bun --env-file=../../.env src/seed/index.ts",
    "db:seed:reset": "bun --env-file=../../.env src/seed/index.ts --reset",
    "db:seed:dashboard-demo": "bun --env-file=../../.env src/scripts/seed-volunteer-dashboard-demo.ts",
    "db:setup-test": "bun --env-file=../../.env src/setup-test-db.ts",
    "db:clean": "bun --env-file=../../.env src/scripts/clean-db.ts",
    "typecheck": "tsc --noEmit",
    "test:integration": "bun --env-file=../../.env vitest run",
    "test": "bun run test:integration"
  },
  "dependencies": {
    "@church/env": "workspace:*",
    "date-fns": "^4.1.0",
    "date-fns-tz": "^3.2.0",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "pg": "^8.17.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@church/config": "workspace:*",
    "@faker-js/faker": "^10.4.0",
    "@types/pg": "catalog:",
    "better-auth": "1.5.5",
    "drizzle-kit": "^0.31.8",
    "typescript": "^5",
    "vitest": "catalog:"
  }
}
